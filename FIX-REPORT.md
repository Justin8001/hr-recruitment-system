# תיקוני סקירת הקוד — 17.09.2026

## היקף

לפי בקשת המשתמש, אין שינוי בהרשאות לפי תפקיד: המערכת מיועדת למנהלת משאבי אנוש יחידה. הגישה לנתוני מועמדים ממשיכה לדרוש התחברות.

## תיקונים

- ייבוא: בדיקה מקדימה ללא כתיבה או קידום רצפים, דוח לכל שורה, אישור קשור לתוכן ולמצב מסד הנתונים, עצירה במקרה של התאמה עמומה, מזהה מקור חובה, שמירת כמה הגשות לאדם, ללא זיהוי לפי שם בלבד. שיוך למשרה רק באמצעות מזהה שנבחר במפורש.
- שמירה: גרסאות נפרדות לאדם ולהגשה, נעילה וטרנזקציה, דחיית עריכה ישנה גם בין שתי הגשות של אותו אדם. לקוח ישן שאינו שולח גרסאות מקבל דרישה לרענון.
- יצירה: כל שדות הכרטיס והאירועים נשמרים באותה טרנזקציה ובבקשה אחת. הוספת הגשה לאדם קיים אינה מוחקת את פרטיו המלאים בשדות ריקים.
- היסטוריה: קריאת כמה מקטעים, איחוד מזהים חוזרים, שימור מקטעים פגומים/לא שלמים והצגת אזהרה. ניתן לשמור שדות אחרים בלי לאבד את המקור הפגום. אירוע החלפת משרה מצביע למשרה החדשה וכולל את שתי התמונות. מצב התחלתי אינו מקבל תאריך אירוע מומצא.
- אחסון ההיסטוריה נשאר בשדה notes לצורך תאימות; אין המרה הרסנית לטבלת אירועים. הפגמים שנמצאו בפרסור תוקנו ונבדקו.
- תיקון שמות: בחירה דטרמיניסטית לפי עברית, תאריך ניתוח ומזהה; שם עברי קיים נשמר. פרטי קשר שסותרים אדם אחר אינם נדרסים.
- כפילויות: טלפונים ישראליים מקומיים ועם קידומת 972 מנורמלים; זיהוי/יצירה/עריכת פרטי קשר/סריקה/ייבוא/איחוד משתמשים בנעילה משותפת. התאמת אימייל וטלפון לאנשים שונים נעצרת.
- איחוד לפי טלפון: שומר את כל ההגשות, מעדיף עברית, משמר דגלי אי־העסקה וסיבותיהם. תעודות זהות סותרות מונעות איחוד. כל הרשומות המקוריות והשיוכים נשמרים בארכיון; ביטול איחוד אפשרי כל עוד לא בוצעו עריכות נוספות.
- אבטחה: התחברות ב־HttpOnly cookie דרך proxy באותו אתר; אין JWT ב־localStorage. כותרת CSRF, CORS לפי כתובת מוגדרת, CSP ללא JavaScript inline או eval, תסריטים חיצוניים ומאזיני אירועים מפורשים. אסימוני OAuth מופרדים מאסימוני התחברות ומשמשים פעם אחת.
- בדיקות קלט: מזהים, תאריכים, שדות, בוליאנים, שכר, הגדרות ומגבלות גודל. קבצים נבדקים לפי החתימה הבינארית, קידוד וגודל, עם הגבלה לתוכן DOCX פרוס.
- תפעול: סוד JWT נבדק בהפעלה; חשבון קיים אינו משנה סיסמה בהפעלה; מיגרציות אינן תלויות בתיקיית ההרצה, נכשלות אם הקבצים חסרים וננעלות בין instances. שגיאות כוללות קוד ומזהה בקשה, בלי לוג של קו״ח, פרטי SQL או אסימונים.
- התאמות: מעדיפות הגשה למשרה הנבדקת, סדר יציב, מגבלת 1–100 תוצאות.
- health: מחזיר תקינות בלבד.
- תלויות: bcrypt 6, Express ותלויות עקיפות מעודכנות. npm audit לאחר העדכון: 0 חולשות מדווחות.

## בדיקות

- `node tests/core.test.cjs`: תהליכים, הסתעפויות, אירועי עבר, שלב הבא.
- `node tests/history.test.cjs`: היסטוריה מצטברת, מניעת החלפת אירועים, מטא־דאטה של משרה, בלוקים פגומים/כפולים/לא שלמים.
- `node tests/security.test.cjs`: חתימות קבצים, קידוד וגודל, משתני סביבה, נרמול טלפונים, סתירות זהות, ייבוא עמום ו־CSP.
- `node tests/integration.test.cjs`: PostgreSQL מבודד בלבד, מיגרציות מקבילות, bootstrap, התחברות, 401/403/409/428/413, יצירה אטומית, שתי לשוניות/הגשות, תחרות ביצירה ובסריקה, ייבוא רב־הגשות ותצוגה מקדימה, OAuth חוזר, רלוונטיות התאמות ואיחוד/ביטול ללא איבוד הגשות.
- בדיקת דפדפן מקומית תחת CSP: התחברות, יצירה, רענון ועריכה.

## דרישות פריסה

יש לפרוס שרת וממשק יחד: הממשק החדש משתמש ב־/api דרך Netlify, וקובץ `_redirects` מעביר לשרת Render הקיים. `FRONTEND_URL` חייב להיות `https://hrforhb.netlify.app`, ו־`JWT_SECRET` לפחות 32 בתים. משתמשים יידרשו להתחבר מחדש. מיגרציה 008 מוסיפה גרסאות וארכיון איחוד; אינה מוחקת נתונים קיימים.

לפני איחוד נתוני ייצור: גיבוי, תצוגת התאמות, בדיקת סתירות, איחוד ושמירת מספרי הארכיון. לאחר האיחוד יש להשוות את כמות ומזהי ההגשות ואת תוכן ההיסטוריה. אם נדרש rollback של הקוד, התוספות לסכמת DB נשארות תואמות לקוד הקודם; חזרה לקוד קודם מחזירה גם את מגבלות האבטחה שלו.
