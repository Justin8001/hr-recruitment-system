# עדכון 17.09.2026

תיקוני הסקירה והבדיקות מתועדים ב־FIX-REPORT.md. סעיפי הגרסה הקודמת להלן הם תיעוד היסטורי; הוראות הפריסה החדשות גוברות.

# עדכון מערכת הגיוס — 16.09.2026

קוד הממשק המעודכן נמצא ב־index.html. קוד השרת לעדכון נמצא בתיקיית backend, והבדיקות בתיקיית tests.

- כל אירוע כולל תאריך, משרה ולקוח כפי שהיו בעת התיעוד, תוצאה וסיכום אינטראקציה.
- התוצאות והשלבים הבאים מבוססים על קובץ ה־Flow שסופק. תוצאה שממשיכה תהליך פותחת את השלב הבא למילוי, גם לאחר שמירה ופתיחה מחדש. השלב הבא אינו נרשם כאירוע שבוצע עד שמתעדים אותו.
- תוצאה של פסילה פותחת סיבות רלוונטיות. נדרשים סיבה ותיעוד. לא נוסף גיל כעילת פסילה; נוספה אי־עמידה בדרישות הפיזיות החיוניות לתפקיד, לבדיקה פרטנית ותיעוד.
- אירוע עם תאריך עבר אינו משנה את המצב הנוכחי כברירת מחדל.
- ההיסטוריה נשמרת ברמת ההגשה ומוצגת מכל ההגשות של אותו personId. נשמרים ערכים לפני ואחרי, כולל הערות וסיבת פסילה. מופיעה התראה בעת שיוך למשרה או ללקוח עם תהליך קודם שנסגר.
- השרת נועל את הרשומה בעת השמירה, משמר אירועים קיימים ומונע דריסה מכרטיס ישן. אין שינוי בסכמת מסד הנתונים. האירועים ממשיכים להישמר במבנה התואם לשדה notes הקיים.
- שינויים ישנים שכבר נדרסו אינם ניתנים לשחזור ללא מקור היסטורי. מצב התחלתי מתועד ומסומן בבירור כמצב הידוע בעת תחילת התיעוד, לא כהיסטוריה מלאה משוחזרת.
- שיוך למשרה וסיווג מקצועי ידניים בלבד. גם בקשה מדפדפן ישן אינה מפעילה שיוך אוטומטי בשרת המעודכן.
- בדיקת תקלה ב־Gemini והחלפת המפתח הושהו לבקשת המשתמש. המפתח מוגדר רק ב־Render, במשתנה GEMINI_API_KEY.

## בדיקות

`node tests/core.test.cjs`

`cd backend && npm ci && npm run build`

`node tests/history.test.cjs` (מהתיקייה הראשית לאחר בניית השרת)

בדיקת ממשק מקומית עם נתוני דמה: `node tests/preview-server.cjs`. הבדיקה שומרת נתוני דמה בזיכרון בלבד, ולא פונה לשרת הייצור.

## פריסה

`node scripts/build-site.cjs` מכין רק את קובצי הממשק בתיקיית web. יש להעלות ל־Netlify את תוכן web בלבד, ולא את תיקיית הפרויקט הכוללת את קוד השרת והבדיקות.

מאגר שרת הייצור: Justin8001/hr-recruitment-system, ענף main, שירות Render hr-recruitment-system. יש לפרוס את תיקוני השרת בנוסף לממשק כדי להפעיל הגנה מפני דריסה ושיוך אוטומטי מדפדפנים ישנים.
